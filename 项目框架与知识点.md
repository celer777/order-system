# 项目框架与知识点

## 整体架构

浏览器/Postman/Swagger → Controller → Service → Mapper → MySQL
↓
Redis


## 餐厅版比喻

| 角色    | 对应代码 | 职责 |
|-------|------|------|
| 老板    | 项目负责人 | 统筹整个饭店（项目）|
| 规矩    | Config | 工作台怎么摆、汤用什么碗装 |
| 服务员   | Controller | 端托盘接待顾客，传菜单 |
| 托盘    | Result | 统一餐具，成功放菜、失败放纸条 |
| 经理    | GlobalExceptionHandler | 厨房出问题，写纸条说明原因 |
| 厨师    | Service | 做菜，先翻手边，没有就叫人去仓库拿 |
| 仓库管理员 | Mapper | 去总仓库取食材 |
| 总仓库   | MySQL | 所有食材永久存放的地方 |
| 工作台   | Redis | 常用食材放手边，用完过期就扔 |
| 菜谱    | Entity | 规定每道菜需要哪些食材 |
| 开业手册  | application.yml | 写清楚总仓库在哪、工作台在哪 |


## 分层职责

### Controller 层（控制器）
- 接收前端请求
- 调用 Service 处理业务
- 返回 `Result` 统一格式给前端

像介于前端和后端之间的**服务员**：前端点菜，后端做菜，Controller 负责把菜端上去。

### Service 层（业务逻辑）
- 写具体的业务规则
- 调用 Mapper 操作数据库
- 处理 Redis 缓存读写
- 抛出 RuntimeException 会被全局异常处理器捕获

像一个**厨师**，接到 Controller 的做菜指令后，手里没有相应的食材，先去旁边的工作台（Redis）里找，没找到就通知 Mapper 去 MySQL 里找。

### Mapper 层（数据访问）
- 继承 MyBatis-Plus 的 BaseMapper，自带增删改查
- 自定义 SQL 实现联表查询

像一个**跑腿的仓库管理员**，把指令告诉它，它就帮你操作总仓库。

### Entity 层（实体类）
- 一个实体类对应一张数据库表
- `@TableName` 指定表名
- `@TableId(type = IdType.AUTO)` 表示主键自增

像一道**菜谱**，规定了这道菜由哪些食材组成。比如 User 这道菜由 id、username、phone、createTime 四样食材组成。

### Common 层（通用工具）
- `Result<T>`：统一返回 `{ code, message, data }` 格式
- `GlobalExceptionHandler`：`@RestControllerAdvice` 全局拦截异常

**Result** 像服务员手里端的**托盘**，上面可以放菜，也可以放写着错误信息的小纸条。

**GlobalExceptionHandler** 就像一个**经理**。厨师发现"没有食材了"，经理赶紧写张纸条，放托盘上。顾客（前端）看到托盘就知道出什么问题了，老板（开发者）也能在旁边看到。没有经理的话，服务员端个空盘子，顾客和老板只能跑进厨房，从一堆代码里翻找问题。

### Config 层（配置）
- `RedisConfig`：配置 Redis 的序列化方式
- `JacksonConfig`：解决 LocalDateTime 转 JSON 的问题

像饭店里的**规矩**，可以是早就定好的，也可以根据实际情况改。比如工作台摆放标准：标签用文字写、食材用 JSON 格式存放（RedisConfig）。又比如 createTime 是汤类食材，直接放盘子里会洒，就用碗装起来再放盘子里（JacksonConfig）。


## 核心调用流程

### 客人点一份"用户1号套餐"

1. 客人（浏览器）喊：来一份用户1号套餐
2. 服务员（Controller）端起托盘（Result），记下需求
3. 厨师（Service）看菜谱（Entity），确认这道菜要哪些食材
4. 厨师先翻手边工作台（Redis）
    - 找到了 → 直接用，不用麻烦仓库
    - 没找到 → 叫仓库管理员（Mapper）去总仓库（MySQL）拿
5. 食材拿到，顺手放工作台上，贴个标签"30分钟后扔"
6. 厨师做好，放托盘上
7. 服务员端给客人

code":200, "message":"成功", "data":{"id":1, "username":"zhangsan", ...

### 客人注册一个已存在的用户名

1. 客人递上菜单：我要注册"张三"
2. 服务员端托盘，传菜单给后厨
3. 厨师查工作台、查总仓库，发现"张三"已经存在
4. 厨师喊：做不了！
5. 经理赶紧过来，写张纸条"用户名已存在"，放托盘上
6. 服务员端给客人

"code":500, "message":"用户名已存在", "data":null

## 用到的主要技术

| 技术 | 做什么 |
|------|------|
| Spring Boot | 项目框架，管理对象，内嵌服务器 |
| MyBatis-Plus | 操作数据库，自带增删改查 |
| MySQL | 存储用户和订单数据 |
| Redis | 缓存，减少数据库查询 |
| Swagger | 在线接口文档，页面可直接测试 |
| Maven | 管理依赖，下载 jar 包 |
| Git | 版本管理，代码上传 GitHub |

Spring Boot 就是开餐厅的连锁加盟体系，只需要写菜单（业务代码），剩下的设备、流程、管理它都帮忙搞定了


## 接口列表

| 方法 | 地址 | 功能 |
|------|------|------|
| GET | /user/{id} | 查询用户 |
| POST | /user/register | 注册用户 |
| POST | /order/create | 创建订单 |
| GET | /order/user/{userId} | 查用户所有订单 |


## 缓存策略

| 数据 | 过期时间 | 原因 |
|------|------|------|
| 用户信息 | 30 分钟 | 用户信息不常变 |
| 订单列表 | 10 分钟 | 订单变化相对频繁 |

创建新订单时会删除该用户的订单缓存，确保数据一致。

