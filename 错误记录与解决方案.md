# 错误记录与解决方案

## Spring Boot 版本兼容性问题

### 1. Invalid value type for attribute 'factoryBeanObjectType'
- **原因**：MyBatis-Plus 与当前 Spring Boot 版本不兼容
- **解决**：降级 Spring Boot 到 3.2.5，升级 MyBatis-Plus 到 3.5.5

### 2. com.sun.tools.javac.code.TypeTag :: UNKNOWN
- **原因**：Lombok 版本与 JDK 17 冲突
- **解决**：升级 Lombok 到 1.18.34，删除 MyBatisPlusConfig，去掉 @MapperScan


## 数据库相关错误

### 3. Unsupported character encoding 'utf8mb4'
- **原因**：MySQL 驱动不认识 URL 中的 utf8mb4
- **解决**：URL 中改为 `characterEncoding=UTF-8`

### 4. Public Key Retrieval is not allowed
- **原因**：MySQL 8 默认不允许直接获取公钥
- **解决**：URL 中加上 `&allowPublicKeyRetrieval=true`

### 5. Table 'order_system.user' doesn't exist
- **原因**：测试时误删了表
- **解决**：重新执行建表 SQL，重新插入测试数据

### 6. order 表插入失败：SQL syntax error near 'order'
- **原因**：order 是 MySQL 关键字
- **解决**：实体类加 `@TableName("order_t")` 指定表名


## Redis 相关错误

### 7. Could not read JSON: Java 8 date/time type not supported
- **原因**：Redis 缓存了旧格式数据，LocalDateTime 序列化不兼容
- **解决**：执行 `redis-cli → flushall` 清空缓存；实体类加 @JsonSerialize 注解


## 端口占用

### 8. Port 8080 was already in use
- **原因**：上次的程序没有完全关闭
- **解决**：
    - 方法一：管理员身份打开 cmd → `netstat -ano | findstr :8080` → `taskkill /PID xxx /F`
    - 方法二：修改 application.yml 端口为 8081
- **正常关闭顺序**：先点红色方块 ⬛ 停止项目 → 等显示 Process finished → 再关 IDEA


## 开发心得

1. **版本很重要**：Spring Boot、MyBatis-Plus、Lombok、JDK 之间的版本要匹配
2. **@TableName 不能忘**：表名是关键字时必须加注解
3. **缓存过期策略**：不同类型设置不同过期时间，数据变更时主动删除缓存
4. **遇到不兼容及时回退**：不硬撑，回退到稳定版本再排查